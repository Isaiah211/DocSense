fastapi>=0.95.0
uvicorn[standard]>=0.22.0
sentence-transformers>=2.2.0
# Back-end for sentence-transformers (required). Pick CPU or GPU build per your machine.
torch>=2.0.0

# Core data / ML libs used by the scripts
numpy>=1.24.0
pandas>=1.5.0
scikit-learn>=1.2.0
tensorflow>=2.12.0

# HTTP / testing
requests>=2.28.0
pytest>=7.0.0
pytest-mock>=3.10.0

# Optional / extras (not required by core scripts but useful for extensions)
# Note: some of these may require platform-specific installs (conda on Windows for faiss)
# faiss-cpu
# python-multipart
# pdfminer.six
# python-docx
# pytesseract
# spacy